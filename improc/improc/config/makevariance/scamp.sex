CATALOG_TYPE    FITS_LDAC      # "ASCII_HEAD","ASCII","FITS_1.0" or "FITS_LDAC"
MAG_ZEROPOINT   27.5
DETECT_THRESH    3.0            # <sigmas> or <threshold>,<ZP> in mag.arcsec-2
ANALYSIS_THRESH  5.0            # <sigmas> or <threshold>,<ZP> in mag.arcsec-2
DETECT_MINAREA   5            # minimum number of pixels above threshold

DEBLEND_NTHRESH 32              # Number of deblending sub-thresholds
DEBLEND_MINCONT 0.005           # Minimum contrast parameter for deblending

BACK_SIZE       64              # Background mesh: <size> or <width>,<height>
BACK_FILTERSIZE 3               # Background filter: <size> or <width>,<height>

BACKPHOTO_TYPE  GLOBAL          # can be "GLOBAL" or "LOCAL"

MASK_TYPE        CORRECT        # type of detection MASKing: can be one of
                                # NONE, BLANK or CORRECT
FLAG_TYPE        OR

#-------------------------------- WEIGHTing ----------------------------------

NTHREADS          1

CHECKIMAGE_TYPE  BACKGROUND_RMS # can be NONE, BACKGROUND, BACKGROUND_RMS,
                                # MINIBACKGROUND, MINIBACK_RMS, -BACKGROUND,
                                # FILTERED, OBJECTS, -OBJECTS, SEGMENTATION,
                                # or APERTURES
CHECKIMAGE_NAME  check.fits     # Filename for the check-image


PARAMETERS_NAME /global/cscratch1/sd/dgold/glens18b/lensgrinder/improc/improc/config/makevariance/scamp.param
FILTER_NAME     /global/cscratch1/sd/dgold/glens18b/lensgrinder/improc/improc/config/makevariance/default.conv
STARNNW_NAME    /global/cscratch1/sd/dgold/glens18b/lensgrinder/improc/improc/config/makevariance/default.nnw
